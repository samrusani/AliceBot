import { WorkspaceLoading } from "../../components/workspace-loading";

export default function Loading() {
  return (
    <WorkspaceLoading
      eyebrow="vNext"
      title="Alice vNext brain workspace"
      description="Loading the governed second-brain workspace and operator controls."
    />
  );
}
