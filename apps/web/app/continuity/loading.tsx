import { WorkspaceLoading } from "../../components/workspace-loading";

export default function Loading() {
  return (
    <WorkspaceLoading
      eyebrow="Continuity"
      title="Continuity workspace"
      description="Loading independent inbox, recall, brief, open-loop, health, and review surfaces."
    />
  );
}
