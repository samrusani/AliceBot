# Alice v0.15.7 — published artifact SHA-256 digests
#
# These are the exact digests of the wheel and source distribution published
# to PyPI for v0.15.7 through GitHub Trusted Publishing. They also appear on the
# project's PyPI page and on the v0.15.7 GitHub Release assets, and are bound to
# this repository, the v0.15.7 tag, the publishing workflow, the exact commit,
# and the protected pypi environment by PyPI's integrity attestations (the
# /provenance endpoints).
#
# Linux verify:  sha256sum -c v0.15.7-checksums.txt
# macOS verify:  shasum -a 256 -c v0.15.7-checksums.txt
# Run either command beside the downloaded files.
#
# Recorded after verifying the same digests from three independent sources:
# the PyPI version-specific JSON API, the SHA256SUMS asset on the GitHub
# Release, and a fresh download hashed locally. All three agree.
#
d61b4bcdafb98b7d3feb0da1dd8f52e3b0a39d0a9703173eb917e17625400a62  alice_memory-0.15.7-py3-none-any.whl
dad1d553a6da64d7f535d291d11ba83d5aea463fa3abe3ffe5abf678b2eca184  alice_memory-0.15.7.tar.gz
